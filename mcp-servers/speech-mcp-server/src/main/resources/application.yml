server:
  port: 8084

spring:
  application:
    name: speech-mcp-server
  ai:
    mcp:
      server:
        name: speech-mcp-server
        version: 1.0.0

speech:
  provider: ${SPEECH_PROVIDER:openai}

OPENAI_API_KEY: ${OPENAI_API_KEY:}

logging:
  level:
    com.buddyai: INFO
